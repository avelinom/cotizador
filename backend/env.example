# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cotizador_db
DB_USER=postgres
DB_PASSWORD=
DB_SSL=false

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# CORS
CORS_ORIGIN=http://localhost:3006

# Google Drive Service Account Configuration
# Estas credenciales se obtienen de un Service Account en Google Cloud Console
# Para obtenerlas:
# 1. Ve a https://console.cloud.google.com/
# 2. Crea un nuevo proyecto o selecciona uno existente
# 3. Habilita las APIs: Google Drive API y Google Docs API
# 4. Ve a "IAM & Admin" > "Service Accounts"
# 5. Crea un nuevo Service Account o usa uno existente
# 6. Crea una clave JSON para el Service Account
# 7. Descarga el archivo JSON y extrae los siguientes valores:
#    - project_id -> GOOGLE_PROJECT_ID
#    - client_email -> GOOGLE_CLIENT_EMAIL
#    - private_key -> GOOGLE_PRIVATE_KEY (reemplaza saltos de línea con \n)
# Ver GOOGLE_DRIVE_SETUP.md para instrucciones detalladas

# Project ID del Service Account
GOOGLE_PROJECT_ID=your-project-id

# Email del Service Account (formato: nombre@proyecto.iam.gserviceaccount.com)
GOOGLE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Private Key del Service Account (debe incluir \n para los saltos de línea)
# IMPORTANTE: La clave privada debe estar en formato de una sola línea con \n para saltos de línea
# Ejemplo: "-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n"
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Opcional: Private Key ID (del archivo JSON del Service Account)
GOOGLE_PRIVATE_KEY_ID=

# Opcional: Client ID (del archivo JSON del Service Account)
GOOGLE_CLIENT_ID=

# Opcional: Client X509 Cert URL (del archivo JSON del Service Account)
GOOGLE_CLIENT_CERT_URL=

# Google Drive Folder IDs (opcional)
# ID de la carpeta donde se guardarán las propuestas
GOOGLE_DRIVE_FOLDER_ID=
